# RemediationBacklog

> Append-only remediation ledger. Do not edit or remove previous entries; add a new dated row when a status changes.

| Status | ID | Area | Finding / impact | Remediation or evidence | Required next check |
|---|---|---|---|---|---|
| 🟢 Fixed — installed | RB-001 | Transfer refresh | Full Refresh only scanned the immediate root. Changed files inside collapsed local or remote folders were unknown until the folder was expanded. | Initial and forced Transfer scans now recursively compare paired local and remote trees. Expanding a folder only changes the rendered tree state and can no longer replace the scan with a partial request. Cache schema moved to v2 so incomplete legacy results are discarded. 27 automated tests pass. | Reload VS Code Insiders; on the configured test host, change files in collapsed nested folders on each side, use **Transfer → Full refresh**, and confirm both appear without expansion. |
| 🟢 Fixed — installed | RB-002 | Transfer watcher refresh | A remote watcher event used the post-download shortcut, which could copy remote metadata into the local record and falsely show files as equal. | Watcher events now invalidate directory listings and queue one debounced, complete paired rescan. The UI updates only from freshly listed local and remote metadata. Contract test added. | Perform a remote-only nested-file edit with Auto Sync download disabled; verify Transfer reports the difference rather than `same`. |
| 🟢 Fixed — installed | RB-003 | Transfer comparison | A local file and remote folder at the same relative path could be classified using only one side's type. | Paired metadata now retains both types; classifier returns `type-changed`; recursive traversal visits a directory present on either side. Unit test added. | Create a file/folder collision in an isolated test subfolder and confirm it appears as `type-changed`. |
| 🟠 Verification pending | ENV-001 | UI automation | Available browser automation cannot attach to a `vscode-webview://` surface. | Backend/message-contract validation, TypeScript compile, webview syntax, lint, package and installed-bundle SHA-256 verification completed. | Use a reloaded VS Code Insiders window for the three UI checks above. |
| 🔵 Monitored | ENV-002 | UNC tooling | Native lint/package startup from network-backed `node_modules` can appear stalled before tool analysis begins. | Same pinned toolchain/configuration was staged locally for successful lint verification; final VSIX was rebuilt and installed with matching bundle hash. | Keep reporting actual command output; do not treat startup delay as a passing lint/package result. |