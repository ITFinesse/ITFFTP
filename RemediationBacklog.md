# RemediationBacklog

> Append-only remediation ledger. Schema formatting may be updated, but entries are never removed; add a new row when a status changes.
> Area is a broad review category (for example UI, Function, Security, Colour, Grammar, Performance, Reliability, Testing, Tooling, Code Quality, or Data Safety); the specific issue belongs in Finding / impact.

## Signal Legend
**69/77 Open** · **8/77 Closed**

0/77 ⬜ **Queued** — confirmed and waiting to start.
68/77 🟦 **In-progress** — remediation or verification is actively underway.
8/77 🟩 **Completed** — fixed, tested, deployed where applicable, and signed off.
1/77 🟥 **Blocked — reason** — cannot complete until the stated dependency or authority is available.


Signal | Priority | ID | Area | Finding / impact | Remediation or evidence | Required next check |
---|---|---|---|---|---|---|
🟩 **Completed** | P0 | RB-001 | Function | Full Refresh only scanned the immediate root. Changed files inside collapsed local or remote folders were unknown until the folder was expanded. | Initial and forced Transfer scans now recursively compare paired local and remote trees. Expanding a folder only changes the rendered tree state and can no longer replace the scan with a partial request. Cache schema moved to v2 so incomplete legacy results are discarded. 27 automated tests pass. | Reload VS Code Insiders; on the configured test host, change files in collapsed nested folders on each side, use **Transfer → Full refresh**, and confirm both appear without expansion. |
🟩 **Completed** | P0 | RB-002 | Function | A remote watcher event used the post-download shortcut, which could copy remote metadata into the local record and falsely show files as equal. | Watcher events now invalidate directory listings and queue one debounced, complete paired rescan. The UI updates only from freshly listed local and remote metadata. Contract test added. | Perform a remote-only nested-file edit with Auto Sync download disabled; verify Transfer reports the difference rather than `same`. |
🟩 **Completed** | P0 | RB-003 | Function | A local file and remote folder at the same relative path could be classified using only one side's type. | Paired metadata now retains both types; classifier returns `type-changed`; recursive traversal visits a directory present on either side. Unit test added. | Create a file/folder collision in an isolated test subfolder and confirm it appears as `type-changed`. |
🟦 **In-progress** | P1 | ENV-001 | Testing | Available browser automation cannot attach to a `vscode-webview://` surface. | Backend/message-contract validation, TypeScript compile, webview syntax, lint, package and installed-bundle SHA-256 verification completed. | Use a reloaded VS Code Insiders window for the three UI checks above. |
🟥 **Blocked — native UNC tool startup** | P3 | ENV-002 | Tooling | Native lint/package startup from network-backed `node_modules` can appear stalled before tool analysis begins. | Same pinned toolchain/configuration was staged locally for successful lint verification; final VSIX was rebuilt and installed with matching bundle hash. | Keep reporting actual command output; do not treat startup delay as a passing lint/package result. |
🟩 **Completed** | P0 | RB-004 | Function | Nested uploads could fail with FTP `553` when a parent directory did not yet exist, then misleadingly report that the upload target did not exist. | FTP missing-path responses are classified separately from connection failures, and parent directories are created before upload. Regression coverage recognises `550`/`553` missing paths without hiding permission errors. Real nested and 120-file FTP uploads completed. | Repeat a nested upload after any FTP server upgrade and confirm the parent path is created once without reconnect loops. |
🟩 **Completed** | P0 | RB-005 | Function | Upload collision/update comparison could use queued source metadata instead of the authoritative remote stat, producing false match or overwrite decisions. | Upload comparison now reads the remote target size from `connection.stat()` and compares it with the current local filesystem size. Real bulk upload/download SHA-256 verification passed. | Add a focused transfer-manager unit test for equal-size, different-size, and missing remote targets. |
🟩 **Completed** | P0 | RB-006 | Reliability | A file downloaded by ITFFTP could trigger an immediate upload feedback loop; a broad time-only suppression could also swallow a genuine user edit made just afterward. | Suppression now records a file signature and is consumed once. ITFFTP-generated writes are ignored, while a subsequent real edit with a different signature is processed. Unit and real watcher checks passed with zero feedback transfers. | Keep the signature test in the required suite whenever download/write handling changes. |
🟩 **Completed** | P0 | RB-007 | Reliability | VS Code can save, pause, and save again; the second edit could be lost or merged incorrectly around the one-second cooldown. | Local watcher debounce tracks the latest event per path and waits for one full quiet second. Real edit → pause → edit testing produced three intended uploads with no duplicate feedback. | Repeat with rapid atomic-save editors and rename-on-save plugins. |
🟩 **Completed** | P1 | RB-008 | Function | Remote changes were not continuously reflected in Transfer/Remote Explorer and nested remote paths required explicit browsing. | A recursive remote metadata snapshot is polled through the active connection; create/change/delete events wait for one quiet second, optionally download, and notify both settings and remote-tree providers. Real remote edit/download/UI notification checks passed. | Stress-test a large remote tree and a burst of changes across several sibling folders. |
🟥 **Blocked — no configured SFTP test host** | P1 | RB-009 | Reliability | `_rename` and `_writeFile` used async Promise executors; exceptions thrown after `await` could bypass the Promise rejection path and leave an operation hanging. | Both methods are now normal async functions with small callback Promises only around the SSH2 operation. Compile, lint, and automated tests pass. | Run live SFTP rename and write tests against a configured SFTP host, including missing parent and permission-denied failures. |
🟦 **In-progress** | P2 | RB-010 | Function | Quick Search and Quick Search Change Path commands were created but omitted from the returned disposable list, risking leaked or unavailable command registrations. | Both registrations are included in the command disposables. Compile and lint pass. | Reload the installed extension, execute both commands from the Command Palette, then close/reload and confirm they register once. |
🟩 **Completed** | P2 | RB-011 | Tooling | Explicit command/language activation events duplicated contribution-generated events and produced VS Code diagnostics. | Redundant activation events were removed; the installed manifest contains only `onStartupFinished`, which is needed for watcher/startup behaviour. Installed manifest was read back and verified. | Confirm the two original package diagnostics remain absent after the next VS Code reload. |
🟩 **Completed** | P2 | RB-012 | Testing | Vitest/Vite could not reliably collect tests from the mapped UNC-backed workspace, so a test command could hang or fail before exercising code. | Tests are bundled with the existing esbuild tool into a local temporary directory and executed with Node's native test runner. The repository command completes with 27/27 passing tests. | Keep the standard `npm test -- --run` command in packaging/CI checks on both local and network-backed workspaces. |
⬜ **Queued** | P1 | RB-013 | Code Quality | More than 250 legacy explicit `any` annotations remain around VS Code, FTP/SFTP, command, and webview message boundaries. The lint policy currently permits them, so a clean lint run does not prove those boundaries are type-safe. | Structural lint errors and unused/unsafe constructs were corrected, but replacing dynamic boundary types requires scoped interfaces and runtime validation rather than blind casts. | Inventory `any` by module; start with webview messages, FTP/SFTP errors, and command item types; add narrowing tests before re-enabling `no-explicit-any`. |
🟦 **In-progress** | P1 | RB-014 | Performance | Complete collapsed-folder detection now requires a recursive metadata walk. Correctness is restored, but post-fix duration and cancellation behaviour have not been measured on a very large configured remote tree. | Scans use metadata only, reuse the managed connection, publish progress, respect ignores, and cancel when superseded. Content downloads remain outside normal comparison. | Measure Full Refresh on representative trees at 120, 1,000, and 10,000 paths; record folder listing time, total duration, cancellation latency, and server errors. |
🟩 **Completed** | P0 | RB-005 | Function | Follow-up status: focused size matching coverage was missing. | Added `sizesMatch` coverage for equal, unequal, unavailable, and zero-byte target sizes. Both upload and download comparison paths now use the authoritative remote stat; 28/28 tests and TypeScript compile pass. | Closed unless transfer collision semantics change. |
🟦 **In-progress** | P1 | RB-013 | Code Quality | Follow-up status: strict-boundary remediation has started. | Replaced shared logger payloads with `unknown`, typed FTP TLS options, removed explicit `any` from debounce/JSON/config utilities, and added reusable unknown-error narrowing helpers. The full suite and TypeScript compile remain green. | Continue module inventory, then re-enable `@typescript-eslint/no-explicit-any` as an error and clear every remaining violation. |
🟦 **In-progress** | P1 | RB-013 | Code Quality | SFTP/FTP core boundary follow-up. | Replaced SSH2 connection and callback `any` annotations with the library's `ConnectConfig` and contextual callback types; FTP/SFTP filesystem errors now use shared unknown-error narrowing. Analytics JSON is validated before it becomes stored typed data. | Complete remaining command/provider boundaries, enable strict lint, then compile and run the full suite. |
🟦 **In-progress** | P1 | RB-014 | Performance | Configured FTP test profile now has repeatable isolated bulk coverage. | A 120-file nested tree uploaded in 13.245s and recursively listed 120 files across 16 directory listings in 1.191s. A 1,000-file nested tree uploaded in 110.498s and recursively listed 1,000 files across 121 directory listings in 9.227s. Every remote size matched and edit-pause-edit ended with an exact SHA-256 match. All temporary local and remote trees were removed. | Add a non-destructive 10,000-path comparison/cancellation benchmark; keep live server concurrency at four because eight sessions produced FTP 421. |
🟦 **In-progress** | P1 | RB-015 | UI | A recursive scan superseded by a new refresh returns without reliably clearing its active progress state. A scope change with no immediate replacement can leave Transfer showing `Comparing…` indefinitely. | Generation cancellation is checked between folder operations, but completion/progress ownership is not tracked separately. | Add scan ownership/finalization so only the current generation may publish completion and a cancelled scan clears progress when no replacement owns it; test cancellation during a 10,000-path walk. |
🟦 **In-progress** | P1 | RB-016 | Security | Remote directory and persisted comparison cache identities omit the username. Two accounts using the same protocol, host, port, and remote root can receive each other's stale metadata. | Existing keys isolate endpoint/path but not the non-secret account principal. | Include username in both in-memory and persisted cache identities, bump the cache schema, and verify two same-host accounts never share rows. |
🟦 **In-progress** | P0 | RB-017 | Security | Folder refresh messages and server-returned entry names are not fully validated before joining them to the configured remote root. `..` or slash-containing names can cause a scan outside the selected root. | Local folder inputs reject traversal, but equivalent validation is missing on the recursive remote boundary. | Add one shared remote-relative validator; reject dot segments, traversal, separators in entry names, and paths outside the configured root; add malicious-name/path tests. |
🟦 **In-progress** | P0 | RB-018 | Reliability | Testing a host uses the manager-owned primary connection and then disconnects it. Running Test connection while transfers/watchers are active can terminate the live session and pooled work. | Test cleanup does not distinguish a newly created test session from an existing managed session. | Use a distinct short-lived connection for tests, or reuse an existing connection without disconnecting it; verify active transfers/watchers survive a test. |
🟦 **In-progress** | P0 | RB-019 | Reliability | Recursive FTP retry calls `connectionManager.connect()` after a closed-session error, but the manager can return the same object when its `connected` flag is stale. The retry can fail identically instead of replacing the dead transport. | Connection recovery lacks an explicit retire-and-reconnect operation scoped to one failed connection. | Add manager-owned invalidation/reconnect semantics without marking a manual disconnect or draining unrelated transfers; cover FTP and SFTP closed-session recovery. |
🟦 **In-progress** | P0 | RB-020 | Function | Recursive comparison descends when either peer is a directory, but some FTP servers report a file-side `LIST` attempt as `not a directory` or `failed to change directory`. Those responses are not treated as an empty side, so a valid `type-changed` subtree can abort the full refresh. | Local file-side reads already become an empty directory listing; the remote missing-side classifier covers only a narrower set of `550` messages. | Classify the non-directory `LIST` responses as an empty side only during descendant traversal and add a collision regression contract. |
🟦 **In-progress** | P1 | RB-021 | Security | In-memory remote directory entries are filtered before caching, but the key omits the host's ignore-pattern configuration. Switching between same-account host definitions with different ignores can reuse an incorrectly filtered listing. | Persistent cache identity includes ignores, but the in-memory directory cache does not. | Include ignore patterns in the in-memory identity and verify different filter configurations do not share rows. |
🟦 **In-progress** | P0 | RB-022 | Security | Create-folder validation checks `.` and `..` before trimming, and rename accepts dot segments and control characters. Whitespace-wrapped traversal names can therefore be joined to a parent path instead of remaining one safe child name. | Slash checks exist, but trimming and the server-entry validator are not shared by mutation inputs. | Normalize once, require a safe single entry name for create and rename, and cover whitespace-wrapped dot segments and control characters. |
🟦 **In-progress** | P0 | RB-023 | Reliability | A manual disconnect can occur while an initial connect or closed-session recovery is awaiting the transport. Because the manager mapping is temporarily absent, disconnect records no cancellation and the delayed operation can reconnect afterward. | Connection attempts are deduplicated but are not guarded by a per-host lifecycle generation. | Increment a per-key generation on disconnect, reject and close connects that finish against a stale generation, and cover disconnect-during-connect/recovery. |
🟦 **In-progress** | P0 | RB-024 | Reliability | A pooled connection can still be establishing while `drain()` deletes its pool. The late acquire can return a session owned by the detached pool, allowing work after manual disconnect and leaking the transport. | Pool size tracks in-flight connects but pool deletion does not invalidate them. | Add a pool generation/closing token; after connect, reject and close any acquire whose pool was drained or replaced. |
🟦 **In-progress** | P0 | RB-025 | Data Safety | Remote Auto Sync download forces overwrite and can replace an unsynced local edit without checking the local dirty/newer state. | The interactive Sync Down path refuses locally dirty files, but the watcher bypasses that guard. | Before watcher download, compare current local metadata/content state and never overwrite a locally dirty/newer file; surface the conflict in Transfer. |
🟦 **In-progress** | P0 | RB-026 | Reliability | The poll snapshot is advanced before the one-second quiet-period callback runs. If the connection is unavailable when that callback executes, it returns without notifying either UI; later polls see no new change, so the event is lost. | UI notification is coupled to a live connection even when Auto Sync download is off. | Always notify Transfer and Remote Explorer after the quiet period; retry connection-dependent download separately without losing the observed UI change. |
🟦 **In-progress** | P1 | RB-027 | Function | A remote file downloaded by ITFFTP is suppressed in the core upload watcher, but the SettingsPanel has an independent local watcher that always marks the path locally dirty. Transfer can show an equal auto-downloaded file as modified. | Download suppression is not shared with the comparison watcher. | Expose one-shot generated-write suppression to the SettingsPanel or verify bytes during its watcher refresh; add auto-download-to-Transfer coverage. |
🟦 **In-progress** | P1 | RB-028 | Performance | Each nominal one-second poll recursively lists the complete remote tree, and a removed directory can enqueue one callback per descendant. Large trees and deletes amplify server work, timers, and UI refreshes. | Transfer refreshes coalesce later, but remote listing and change scheduling do not. | Measure representative scale, collapse descendant delete events, and use a non-overlapping adaptive poll interval without reducing recursive detection. |
🟦 **In-progress** | P1 | RB-029 | UI | A successful local auto-upload notifies the watcher callback with `side: local`, while Remote Explorer refresh is only triggered for `side: remote`. The remote listing can remain stale after upload. | Transfer receives the change callback, but the second remote-facing consumer is filtered out. | Refresh Remote Explorer for successful local uploads as well as remote changes; add callback contract coverage. |
🟦 **In-progress** | P0 | RB-030 | Reliability | Expected remote writes have no expiry. If an upload is not observed promptly, a later genuine remote edit with the same size and timestamp signature can be consumed as the expected upload. | Local generated-write suppression expires and is one-shot; remote suppression is one-shot but unbounded in time. | Store an expiry with the expected remote signature, prune stale entries, and test same-signature edits after expiry. |
🟦 **In-progress** | P0 | RB-031 | Reliability | Invalidation retires the failed wrapper and suppresses its delayed lifecycle events. If the immediate replacement connect fails, no disconnected handler remains able to schedule automatic reconnect. | Explicit recovery avoids reusing the stale session, but failure exits with the host offline. | Schedule normal auto-reconnect when recovery fails and its lifecycle generation has not been manually cancelled. |
🟦 **In-progress** | P0 | RB-032 | Reliability | A pooled session that reports a classified closed error can remain eligible for reuse, returning a poisoned transport to later transfers. | Closed-session handling must discard the failed pooled connection before replacement work is acquired. | Add a pool discard API and verify the poisoned session is never reused after FTP/SFTP closed-session errors. |
🟦 **In-progress** | P1 | RB-033 | Reliability | Scan pool fallback silently returns the primary connection and violates the strict no-fallback parallel contract, risking concurrent operations on one session. | Recursive scans need distinct leased connections for parallel work. | Add strict pool acquisition that fails or serializes explicitly when a distinct connection is unavailable; cover concurrency limits. |
🟦 **In-progress** | P1 | RB-034 | Reliability | A failed recursive batch can return while sibling connection operations from `Promise.all` are still running, allowing late writes and inconsistent scan state. | Batch failures must settle all sibling operations before propagating the error. | Use allSettled-style batch cleanup, then throw the classified failure; add a sibling-completion regression test. |
🟦 **In-progress** | P2 | RB-035 | Security | Remote root joins can produce paths such as `//name` and are not canonical, making cache keys, containment checks, and server requests inconsistent. | Remote path construction needs one canonical join/normalization boundary. | Use `normalizeRemotePath` for roots and descendants and cover leading separators, empty roots, and dot segments. |
🟦 **In-progress** | P0 | RB-036 | Reliability | A watcher scan bypasses the manual Full-refresh scheduler and can cancel or partially replace an in-flight refresh, leaving incomplete UI state. | Watcher and manual refreshes need one scheduler and generation ownership. | Route both through the unified scheduler and test watcher events during a manual Full refresh. |
🟦 **In-progress** | P0 | RB-037 | Function | Paired files with identical byte counts are classified as unchanged even when their known modification times differ materially, so same-length edits can disappear from Transfer. | Size-only comparison misses a common edit pattern and contradicts directional sync metadata. | Compare known timestamps using an FTP-safe precision tolerance and test same-length local-newer, remote-newer, tolerance, unknown-time, and dirty cases. |
🟦 **In-progress** | P0 | RB-038 | Reliability | Saving a changed host, port, local root, remote root, or ignore policy can leave the previous watcher active and start a second stale observer. | Watchers are keyed by derived host/root details instead of one replaceable workspace owner. | Replace the watcher atomically per workspace whenever configuration changes; verify the old watcher is disposed. |
🟦 **In-progress** | P0 | RB-039 | Data Safety | Two edits to the same path can start overlapping transfers after separate one-second quiet periods, allowing an older upload or download to finish last. | Debounce timers do not serialize in-flight work per path. | Add generation-based, per-path latest-wins execution and test edit, pause, edit while the first operation is running. |
🟦 **In-progress** | P1 | RB-040 | Function | With the global watcher enabled, disabling one transfer direction can prevent that side being observed at all, so Transfer and Remote Explorer miss changes that should still be displayed. | Observation and automatic transfer policy are coupled. | Always observe both sides while globally enabled; gate only transfer actions with explicit Auto Sync direction and test Auto Sync Off UI updates. |
🟦 **In-progress** | P0 | RB-041 | Security | Remote Explorer navigation and mutation handlers can accept paths above the configured remote root; a download from such a path can resolve parent segments outside the configured local root. | The webview path is trusted by navigation, mutation, and local destination joins without one shared containment boundary. | Enforce configured remote-root containment on every handler and local-root containment before downloads; add traversal and Navigate Up tests. |
🟦 **In-progress** | P0 | RB-042 | Reliability | Connection-manager and pool identities omit protocol and may use a display name instead of the host, so two same-named profiles can reuse the wrong server session. | Session identity must be based on protocol, canonical host, port, and username rather than presentation labels. | Replace both keys with the same canonical endpoint identity and test same-name/different-host plus FTP/SFTP separation. |
🟦 **In-progress** | P0 | RB-043 | Function | Transfer classifies a file/folder collision as a directory, excludes it from bulk file sync, and direct actions transfer only descendants without removing the opposing type. | A type-changed path needs an explicit recursive replacement operation before copying the source type. | Implement safe file-to-folder and folder-to-file replacement in both directions and test direct plus bulk sync. |
🟦 **In-progress** | P0 | RB-044 | Data Safety | Default update-mode upload skips when the local source is newer and can overwrite when the remote target is newer, reversing the intended timestamp decision. | The upload comparison inequality is inverted. | Correct the direction, use the shared timestamp tolerance, and test local-newer, remote-newer, equal, and unknown timestamps. |
🟦 **In-progress** | P0 | RB-045 | Data Safety | Completed transfers do not consistently read back target size or content metadata; downloads do not preserve remote modification time and best-effort upload time is not verified. | A queued transfer can be reported complete without authoritative post-transfer evidence, weakening later comparison accuracy. | Verify target stat and size after every transfer, preserve download timestamps, verify upload timestamp when supported, and exercise both protocols. |
🟦 **In-progress** | P0 | RB-046 | Function | Selecting a host local or remote folder can leave comparison rows from the previous roots visible; a subsequent action sends those stale paths with the newly selected profile. | Host-root pickers update the profile but only Transfer-root targets invalidate and refresh comparison state. | Clear selection, content, and rows and force a paired refresh for every root change before enabling transfer controls. |
🟦 **In-progress** | P2 | RB-047 | UI | Static Ready and workspace dots, autosave text, and an inline message region duplicate transient status inside the layout instead of using the required fading top-centre popup. | Several legacy inline status elements remain even though toast and progress popup infrastructure exists. | Remove inline status elements and route save, connection, error, and progress feedback through the fading top-centre popup or toast region. |
🟦 **In-progress** | P0 | RB-048 | Data Safety | FTP and SFTP downloads write directly over the destination and follow a local symlink-to-file; a failure can leave a partial file in place of the previous copy or write outside the intended file. | Downloads lack same-directory staging, atomic replacement, symlink rejection, cleanup, and authoritative readback. | Download to a unique contained temporary file, verify it, reject unsafe target types, atomically replace the destination, and test failure rollback. |
🟦 **In-progress** | P0 | RB-049 | Data Safety | Uploads write or replace the live remote target without a unique transaction; deterministic SFTP temporary names collide and unlink the old target before the final rename. | Concurrent same-path uploads and rename failures can corrupt or lose the prior remote file. | Serialize same-path writes, use unique remote staging, verify staging, and replace without deleting the live target until a safe rename strategy succeeds. |
🟦 **In-progress** | P0 | RB-050 | Security | Core recursive remote delete accepts sibling or parent paths outside the configured remote root; SFTP can invoke recursive shell deletion after only an exact blacklist check. | Slash normalization is not containment, and child deletion errors can be suppressed after partial removal. | Require configured-root containment in every recursive delete entry point, avoid shell recursive deletion, and propagate child failures with traversal tests. |
🟦 **In-progress** | P0 | RB-051 | Data Safety | Transfer actions trust cached target-existence and type metadata, so a target changed after comparison can be overwritten or falsely skipped without a fresh collision check. | TransferManager re-stats only when callers omit target metadata, while Transfer always supplies cached values. | Re-stat immediately before mutation and treat supplied metadata as display hints only; test create, delete, and type-change races after scanning. |
🟦 **In-progress** | P1 | RB-052 | Function | Collision-policy Skip is converted into a completed fulfilled transfer, so callers count it, clear dirty state, and record analytics despite moving no bytes. | Skipped is represented as success rather than a distinct terminal result. | Add an explicit skipped result/status and ensure sync counts, dirty clearing, queue UI, and analytics exclude skipped items. |
🟦 **In-progress** | P0 | RB-053 | Reliability | Transfer timeout and cancellation update queue state without aborting active protocol I/O; queued promises may never settle and a cancelled operation can still finish later. | Promise racing does not cancel the transport and queue removal is not promise settlement. | Add abortable transfer operations or retire the exact transport, settle every promise once, and test timeout/cancel during active and queued transfers. |
🟦 **In-progress** | P1 | RB-054 | Function | Empty directories are not created during upload and hard traversal caps silently truncate large/deep directory sync while reporting completion. | Recursive enumeration treats protective limits as successful end-of-tree conditions. | Create selected empty roots and descendants, and fail explicitly with a surfaced reason when a configured safety limit is reached. |
🟦 **In-progress** | P0 | RB-055 | Reliability | Remote stat and exists paths can translate permission or transport errors into absent, bypassing collision rules and triggering unsafe creation or overwrite behavior. | FTP and SFTP existence helpers swallow errors that are not genuine missing-path responses. | Return absent only for classified missing errors and propagate permission/connection failures; test FTP and SFTP error classes. |
🟦 **In-progress** | P0 | RB-056 | Reliability | Full Refresh clears the directory cache before it acquires the serialized scan lock; an older in-flight scan can repopulate stale listings afterward and the queued forced scan can reuse them. | Cache invalidation is outside the operation that owns scan ordering. | Move forced invalidation inside the serialized reservation immediately before its run or generation-tag cache entries; test the older-scan repopulation interleaving. |
🟦 **In-progress** | P0 | RB-057 | Data Safety | A delayed comparison-cache write captures a host config but serializes the later mutable global records, so profile switches or a newer partial scan can write one host's state into another host's cache file. | Persist scheduling lacks a record snapshot, cache-key ownership, and generation check. | Snapshot completed records and expected cache identity at scheduling, and discard delayed writes when ownership or generation changes. |
🟦 **In-progress** | P1 | RB-009 | Reliability | Status correction: the configured test profile is a live FTP endpoint rooted at /public_html/test with local root tests, so FTP end-to-end verification is available and must not be represented as blocked. | The FTP profile will cover live UI, transfer, recursion, watcher, and error logging; SFTP-only rename/write behavior requires deterministic protocol fakes because FTP credentials cannot exercise SSH2. | Run the full configured FTP workflow and executable SFTP behavior tests, then sign off the fixed async error propagation without treating the FTP test environment as blocked. |
🟦 **In-progress** | P1 | RB-058 | Function | The contributed sftp.json schema is stale against runtime configuration: it retains StackerFTP copy and mismatched names or shapes for local path, timeout, Remote Explorer order, sync mode, and remotes. | Editor validation and completion can reject valid runtime settings or suggest fields the extension does not consume. | Reconcile the schema with FTPConfig and workspace settings, retain documented backward-compatible aliases only where runtime supports them, and validate representative configured profiles with AJV. |
🟦 **In-progress** | P1 | RB-059 | UI | Transfer tree rows, context menus, folder pickers, and ignore listboxes lack complete ARIA state, roles, focus management, Escape handling, and keyboard operation. | Mouse interaction works, but keyboard and assistive-technology users cannot reliably expand, select, or dismiss the controls. | Add truthful tree/listbox/menu semantics and keyboard focus flows, then verify with source contracts and rendered keyboard interaction. |
🟦 **In-progress** | P1 | RB-060 | Function | Follow folders and Hide ignored are visible toggles whose values are never read, so changing them has no effect. | Only Changed files only currently participates in Transfer rendering. | Implement the promised behavior or remove the controls; retain only truthful, tested UI choices. |
🟦 **In-progress** | P1 | RB-061 | UI | A failed local or remote folder-tree request leaves its loading marker spinning because generic error replies do not identify and clear the pending picker request. | Failure state is not correlated with requestId, kind, and path. | Return a dedicated folder-picker error contract, clear loading, retain the tree, and show the error in the top-centre popup. |
🟦 **In-progress** | P2 | RB-062 | Grammar | Full Refresh is rendered with inconsistent capitalization and the static markup initially says Refresh before JavaScript changes it. | Requested control copy is not exact and can visibly flash the legacy label. | Use the exact text Full Refresh in source markup and runtime with one matching accessible name. |
🟦 **In-progress** | P1 | RB-063 | UI | Both bulk Sync changed controls are in the centre lane and no top-row pair exists, so their relationship to the selected-item Upload and Download actions remains ambiguous. | Bulk and selected-path actions need distinct placement and wording. | Place Sync changed up and Sync changed down in the top toolbar; keep centre Upload, Download, and View explicitly scoped to the selected item. |
🟦 **In-progress** | P0 | RB-064 | Function | The host editor can save FTPS security mode Control, but the schema rejects it and the FTP runtime interprets it as an unencrypted connection. | Align the editor, schema, type, and basic-ftp mapping so Control uses explicit TLS while Implicit retains implicit TLS. | Compile, run focused schema/connection contracts, then connect only to a disposable configured FTPS endpoint if one becomes available; never claim live FTPS proof from the FTP-only test host. |
🟦 **In-progress** | P0 | RB-065 | Data Safety | The host editor derives Auto Sync Down from the separate legacy Download when opened setting, so saving an unrelated setting can silently enable background remote-to-local transfers. | Keep open-triggered downloads independent from Auto Sync; migrate only the legacy upload-on-save direction and preserve Off unless an explicit autoSync value is configured. | Add a UI contract for legacy downloadOnOpen-only, reload the installed dashboard, save an unrelated setting, and confirm Auto Sync remains Off while open-triggered download remains unchanged. |
🟦 **In-progress** | P2 | RB-066 | Function | New remote folder remains enabled with no selected host and sends an undefined connection, producing an avoidable operation error. | Disable profile-bound remote mutation controls whenever no host is selected. | Reload the installed dashboard with no hosts and confirm New remote folder is disabled, then select a host and confirm it becomes available. |
🟦 **In-progress** | P0 | RB-028 | Performance | Follow-up reproduction: the monitor recursively lists the complete remote tree every 1–5 seconds even when idle, then a detected event can enqueue a second forced recursive local-and-remote comparison. | The poll has no inexpensive idle mode and watcher notifications do not carry changed paths into a targeted comparison. | Use a non-overlapping adaptive idle cadence, tolerant stable signatures, and coalesced changed-parent refreshes; prove an idle large tree does not scan continuously and one mutation causes one bounded UI refresh. |
🟦 **In-progress** | P1 | RB-052 | Function | Follow-up reproduction: single, bulk, and folder actions treat a fulfilled skipped TransferOutcome as completed, display Uploaded or Downloaded, and clear dirty state although no bytes moved. | Promise fulfilment is incorrectly used as the transfer-success condition. | Count, announce, and refresh only outcomes whose status is completed; retain skipped rows as out of sync and surface the skip reason in the top-centre popup. |
🟦 **In-progress** | P0 | RB-045 | Reliability | Follow-up reproduction: post-transfer refresh fabricates equality by copying cached source metadata to the target, while an older in-flight scan can later restore the pre-upload mismatch. | Transfer completion is not followed by authoritative target readback and same-generation scans/cache writes can commit stale records. | Re-list and compare affected parents after a completed transfer, revision-guard scan/cache commits, and test a deferred old scan cannot overwrite a successful upload. |
🟦 **In-progress** | P0 | RB-057 | Data Safety | Follow-up reproduction: delayed cache writes and watcher scans are guarded only by profile and scan generation, not by a monotonic data revision, so a same-profile transfer can be overwritten or persisted as stale. | Live transfer mutations do not invalidate older scan results or scheduled cache snapshots. | Add revision ownership to scan commits and persistence, send empty snapshots, and test upload-during-scan, delayed-write, profile-switch, and delete-to-empty interleavings. |
🟦 **In-progress** | P1 | RB-043 | Function | Follow-up reproduction: type-changed rows are selectable for transfer, but replaceTypeCollision is never consumed and fresh file-versus-directory checks always reject the action. | The UI offers an operation for which the transfer layer's authorized replacement path is dead code. | Implement an explicit contained collision-replacement transaction or leave the row unchanged with a truthful actionable reason; verify both upload and download collisions. |
🟦 **In-progress** | P0 | RB-007 | Reliability | Regression reopened: the required save, one-second quiet period, second save sequence has not been proven against the current watcher and upload implementation. | Earlier sign-off predates the now-confirmed skipped-outcome and scan-contention faults, either of which can hide or delay the second version. | Run a real isolated edit, wait across the quiet boundary, edit again, and prove the remote ends with the latest bytes with no duplicate or feedback transfer. |
🟦 **In-progress** | P0 | RB-008 | Function | Regression reopened: remote monitoring continuously rescans the whole tree and a detected change can trigger another full paired scan, making remote UI updates slow and unreliable. | The earlier implementation detected changes but did not bound idle work or preserve interactive transfer priority. | Prove remote create, edit, and delete update Transfer and Remote Explorer once after the quiet period while stable large trees do not scan continuously. |
🟦 **In-progress** | P1 | RB-027 | Function | Follow-up reproduction: a completed same-size Auto Sync upload larger than 5 MiB can remain locally dirty because watcher refresh is not told that the transfer completed and byte verification is intentionally capped. | Carry the completed-transfer path into the authoritative comparison refresh and establish a verified synchronized signature instead of depending on whole-file comparison. | Exercise a 6 MiB same-size edit through Auto Sync and confirm Transfer returns to same after target readback. |
🟦 **In-progress** | P1 | RB-040 | Function | Follow-up reproduction: with Auto Sync Off or no live connection, a new local directory event is emitted without its directory kind because type detection occurs after transfer-policy gating. Its nested contents can be omitted from the targeted UI refresh. | Determine local event kind before connection and transfer-direction checks, then request a recursive targeted refresh for directory events even when no automatic transfer is allowed. | Create a pre-populated nested local folder with Auto Sync Off and confirm one event discovers every descendant in Transfer. |
🟦 **In-progress** | P2 | RB-067 | Tooling | The release include list did not exclude `scripts/**`, so credential-aware live E2E harnesses and internal test runners would be shipped in the VSIX despite having no runtime role. | Exclude the complete scripts directory from extension packaging while keeping the bundled runtime and required webview assets. | Build the final VSIX and inspect its file list to prove no `extension-core-e2e`, `configured-host-e2e`, or test-runner script is present. |
🟦 **In-progress** | P1 | RB-058 | Function | Follow-up evidence: the schema accepts named reusable profiles such as `{ remote: "name" }`, but the Settings parser rejects them before runtime resolution and does not enforce the same field constraints as the schema. | Route settings input through the shared configuration contract so editor validation, reusable remotes, and runtime resolution accept and reject the same shapes. | Exercise direct and named-remote profiles through parser, schema, save, reload, and connection tests without exposing credentials. |
🟦 **In-progress** | P0 | RB-064 | Function | Follow-up evidence: the FTP core and schema distinguish FTPS Control and Implicit modes, but the host editor exposes no TLS-mode control and therefore cannot preserve or deliberately select them. | Add an explicit FTPS TLS mode field that appears only for FTPS and round-trips `control` or `implicit` without weakening FTP/SFTP behavior. | Save and reopen both FTPS modes, validate generated configuration, and run deterministic connection-option tests; live FTPS remains conditional on a disposable endpoint. |
🟦 **In-progress** | P0 | RB-010 | Function | Follow-up reproduction: command registration destructures `remoteExplorer` before activation assigns it to the provider container, so refresh, create, rename, reveal, and related closures permanently retain `undefined` for that session. | Construct and assign provider dependencies before registering commands, or dereference the live container at invocation; keep every command disposable and single-registered. | Activate a fresh Extension Development Host and exercise Remote Explorer refresh, create, rename, reveal, Quick Search, and reload registration once. |
🟦 **In-progress** | P0 | RB-068 | Function | Auxiliary transfer surfaces map local files from the workspace root instead of the selected profile `localPath`; Compare also changes its remote base after refresh. With local root `tests`, the same selection can compare or transfer against two different remote paths. | Centralize profile-root local/remote mapping and use it consistently in Remote Explorer, Quick Search, Compare initialization, refresh, and every action. | On the configured `tests` ↔ `/public_html/test` profile, compare, upload, download, and reveal a nested path from each surface and verify the exact paired target. |
🟦 **In-progress** | P0 | RB-069 | Data Safety | Upload Current File ignores a failed `TextDocument.save()` result and can upload older on-disk bytes while the editor still contains unsaved changes. | Abort with a truthful transient warning when save returns false; enqueue upload only after the document is durably saved. | Force a save rejection/cancellation and prove no transfer is queued, then save successfully and verify the latest bytes arrive remotely. |
🟦 **In-progress** | P1 | RB-052 | Function | Follow-up reproduction: Remote Explorer tree/webview and tree-download commands still ignore skipped or failed transfer outcomes, may count them as success, and may open a local file that was not downloaded. | Apply the shared completed/skipped outcome contract to every remaining caller; open, reveal, refresh, and increment success only after `completed`. | Run skipped single-file and mixed directory actions from Transfer, Remote Explorer tree/webview, and command surfaces and confirm truthful top-centre feedback and unchanged dirty rows. |
🟦 **In-progress** | P0 | RB-070 | Testing | There is no single executable feature matrix proving every contributed command, dashboard option, transfer action, watcher direction, collision policy, sync mode, picker, and explorer surface remains wired and truthful. Individual tests can pass while an option is dead or an end-to-end route is unexercised. | Inventory every user-facing option and feature, bind each to deterministic contract/behavior coverage, and add live configured-host scenarios for the stateful FTP paths. | Prove Auto Sync Off, Upload, Download, and Both; edit-pause-edit; single/bulk/folder upload/download; Full Refresh; roots; remote create/rename/delete; compare/view; collision/sync modes; reconnect; and every reachable command, recording protocol-specific blockers precisely. |
🟦 **In-progress** | P1 | RB-054 | Function | Follow-up evidence: configuration folder discovery silently stops at 10,000 entries or depth 12, Quick Search stops at depth 5, and other explorer traversals cap results without telling the user. Valid deep/large content therefore disappears while the operation appears complete. | Use shared explicit traversal limits with complete results below the limit and a surfaced top-centre error when a safety boundary is reached; remove reduced-depth silent fallbacks. | Exercise deep trees and just-over-limit fixtures in configuration pickers, Quick Search, Transfer, and Remote Explorer and prove complete results or one truthful limit error—never partial success. |
🟦 **In-progress** | P1 | RB-071 | Function | Four visible workspace settings are saved but have no operational branch: Refresh after operations, WebMaster tools, Syntax highlighting, and Use native TreeView. Their controls falsely imply behavior that never changes. | Implement each setting at its owning feature boundary or remove it everywhere when the legacy surface no longer exists; add state-change tests that prove an observable effect. | Toggle each option independently in the installed dashboard, exercise its feature, reload, and confirm both persistence and behavior. |
🟦 **In-progress** | P0 | RB-072 | Data Safety | Auto Sync hard-overrides every profile to full/overwrite, ignoring the selected sync mode and collision policy. In Both mode, simultaneous local and remote edits have no deterministic conflict contract. | Preserve the configured sync and collision policies for watcher transfers and define a latest-generation conflict rule that never silently overwrites two independently dirty sides. | Live-test Off, Upload, Download, and Both with ask/skip/overwrite, update/full, and simultaneous two-sided edits; verify truthful conflicts and latest bytes. |
🟦 **In-progress** | P1 | RB-073 | Reliability | Legacy explorer/queue views are permanently hidden while commands and providers still target or refresh them; anonymous event subscriptions are not disposed, creating dead work and reload-time listener leaks. | Remove unreachable legacy commands/providers or restore one truthful active view, and retain/dispose every event subscription exactly once. | Activate/reload repeatedly, exercise queue and reveal commands, and prove no hidden-provider refreshes, duplicate listeners, or commands targeting unavailable views. |
🟦 **In-progress** | P2 | RB-074 | Grammar | User-facing command names still mix `SFTP:` and `Compare Local and Remote Folders` even though the product supports FTP/FTPS and the viewer is named Transfer. | Rename visible titles consistently to `ITFFTP:` and `Transfer` while retaining historical command IDs for compatibility. | Reload the installed extension and inspect Command Palette, menus, views, and dashboard copy for the exact consistent names. |
🟦 **In-progress** | P1 | RB-075 | Tooling | The prepublish/package path does not enforce compile, lint, tests, or package-content checks, and the repository has no CI workflow. A stale or failing bundle can be shipped. | Add one release gate used by prepublish/package and CI that compiles, lints, tests, bundles, inspects the VSIX, and fails before installation on any error. | Run the gate from the network-backed workspace and CI-compatible local mirror; verify deliberate compile/test/package-content failures stop release. |
